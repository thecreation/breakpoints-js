// 'use strict';

// import UnionSize from '../../src/unionSize';

// describe('UnionSize', () => {
//   let names = 'sm md';

//   let size = new UnionSize(names);

//   describe('new UnionSize()', () => {
//     it('should exists', () => {
//       expect(size).to.exists;
//       expect(size.name).to.equal(name);
//     });

//     it('should initialized after constructor', () => {
//       expect(size.callbacks).to.exists;
//     });
//   });
// });
